// "use strict"; // trat all JS code as newer version

// alert(1+1) -> available on node but with different syntax

let Name = "Harsh"
let age = 22
let isLoggedIn = false 

/*
number
big int
string => preferred in double quotes
null => standalone value
undefined => value not assigned
symbol=> unique (use in react)
object
*/

console.table([
    typeof(isLoggedIn), 
    typeof(Name), 
    typeof(age), 
    typeof(null), 
    typeof(undefined)
])

